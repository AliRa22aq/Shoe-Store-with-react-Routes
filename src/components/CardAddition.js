import React, {useContext} from 'react'
import {CartContext} from './CartContext';

export const CardAddition = (x) => {

    ///const [C, setC] = useContext(CartContext);

    console.log(x);


    return (
        <div>
            
        </div>
    )
}
