import React from 'react'
import { Outlet, Link, Route, Routes } from 'react-router-dom'

function Products() {


    return (
        <div>
                    This will remain
                        <Outlet />

        </div>
    )
}

export default Products
